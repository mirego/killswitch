# The environment for Rails to use
RACK_ENV=test

# The canonical host and port to build URLs in emails
CANONICAL_HOST=localhost

# Mailer defaults
MAILER_FROM=localhost

# Database URL
DATABASE_URL=postgres://postgres:postgres@localhost/killswitch_test
